{
  "name": "lbl-tooling",
  "version": "0.0.0",
  "description": "Repo tooling root (commit-message linting, future release automation) — not published, no runtime code.",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
